
Skip to content
Navigation Menu

Code
Issues
13
BreadcrumbsFlash-Md
/Github-Deploy-File
Latest commit
franceking1
franceking1
last week
History
33 lines (25 loc) · 554 Bytes
File metadata and controls

Code

Blame
name: Node.js CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install -g pm2 && npm i --legacy-peer-deps

    - name: Start application
      run: npm run franceking
Flash-Md/Github-Deploy-File at main · franceking1/Flash-Md 
